# Production origin used by resolveMetadataBase() to resolve absolute URLs
# (og:image, twitter:image) at build time. Next.js loads this file automatically
# during `next build` / `next start`. Not a secret — it's the site's public domain.
NEXT_PUBLIC_SITE_URL=https://ghost-shift-marketing.spacerockapps.com
