# Production origin used to resolve absolute URLs (e.g. og:image, twitter:image)
# in Next.js metadata. Must be set to the deployed site's HTTPS origin with no
# trailing slash.
NEXT_PUBLIC_SITE_URL=https://ghost-shift-marketing.spacerockapps.com
